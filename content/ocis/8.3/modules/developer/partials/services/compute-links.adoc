// provide computed attributes to the calling page
// the attribute 'service_name' must be defined by the calling page to be used here.

// compute 'ocis_git_version' which resolves to 'master' on the prerelease
// and to 'stable-x.y' on a released version
include::admin:partial$multi-location/compose-version.adoc[]

// Create a path to access the service folder
// Use the correct path segment such as master or like stable-8.0
:git-service-root: {compose_url}{ocis_git_version}{dev_ocis_services_path}{service_name}

// Create a path the the service in the admin docs
:service-path-to-admin: admin:{s-path}/{service_name}.adoc
